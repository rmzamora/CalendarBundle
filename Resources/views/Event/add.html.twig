{% extends "RmzamoraCalendarBundle::layout.html.twig" %}
{% block rmzamora_calendar_content %}
<form action="{{ path(rmzamora_calendar_route('event', 'add'), {"calendarId": calendar.getId()}) }}" {{ form_enctype(form) }} method="POST">
    <p>Adding event for calendar {{ calendar.getName() }}. Riveting.</p>
    {{ form_errors(form) }}

    {{ form_rest(form) }}

    <input type="submit" value="Create Event" />
</form>
{% endblock rmzamora_calendar_content %}
