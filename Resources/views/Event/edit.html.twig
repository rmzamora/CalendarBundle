{% extends "RmzamoraCalendarBundle::layout.html.twig" %}
{% block rmzamora_calendar_content %}
<h2>Edit &quot;{{ event.getTitle() }}&quot;</h2>
<form action="{{ path(rmzamora_calendar_route('event', 'edit'), { 'id': event.getId() }) }}" {{ form_enctype(form) }} method="POST">
    {{ form_errors(form) }}

    {{ form_rest(form) }}

    <input type="submit" value="Edit Event" />
</form>
{% endblock rmzamora_calendar_content %}
