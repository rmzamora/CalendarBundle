{% extends "RmzamoraCalendarBundle::layout.html.twig" %}
{% block rmzamora_calendar_content %}
<div class="event_list">
    <p>Listing all your events</p>
    <ul>
        {% for event in events %}
        <li><a href="{{ path(rmzamora_calendar_route('event', 'show'), {'id': event.getId()} ) }}">{{ event.getTitle() }}</li>
        {% endfor %}
    </ul>
</div>
{% endblock rmzamora_calendar_content %}
