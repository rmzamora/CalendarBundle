{% extends "RmzamoraCalendarBundle::layout.html.twig" %}

{% block rmzamora_calendar_content %}
<form method="post">
    <h3>Are you SURE you want to delete {{ attendee.user.username }}'s attendance to {{ attendee.event.title }}? This cannot be undone!</h3>
    <input type="submit" value="Confirm" />
    <a href="{{ path(rmzamora_calendar_route('attendee', 'show'), {'id': attendee.id}) }}">Cancel</a>
</form>
{% endblock rmzamora_calendar_content %}
