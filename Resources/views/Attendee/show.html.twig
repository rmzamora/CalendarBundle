{% extends "RmzamoraCalendarBundle::layout.html.twig" %}
{% block rmzamora_calendar_content %}
<p>Event: {{ attendee.event.title }}</p>
<p>User: {{ attendee.user.username }}</p>
<p>Status: {{ attendee.status }}</p>
<p>
{% if is_granted('EDIT', attendee) %}(<a href="{{ path(rmzamora_calendar_route('attendee', 'edit'), {"id": attendee.id}) }}">edit</a>){% endif %}
{% if is_granted('DELETE', attendee) %}(<a href="{{ path(rmzamora_calendar_route('attendee', 'delete'), {"id": attendee.id}) }}">delete</a>){% endif %}
</p>
{% endblock rmzamora_calendar_content %}
