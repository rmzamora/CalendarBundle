{% extends "RmzamoraCalendarBundle::layout.html.twig" %}
{% block rmzamora_calendar_content %}
<h3>Your calendars:</h3>
<ul class="calendar_list">
{% for calendar in calendars %}
    <li><a href="{{ path(rmzamora_calendar_route('calendar', 'show'), {'id': calendar.getId()} ) }}">{{ calendar.getName() }}</a> (owned by {{ calendar.getOwner() ? calendar.getOwner().getUsername() : 'the community' }})</li>
{% endfor %}
</ul>
<p>{% if calendars is empty %}None yet. {% endif %}<a href="{{ path(rmzamora_calendar_route('calendar', 'add')) }}">Create a new calendar?</a></p>
{% endblock rmzamora_calendar_content %}
